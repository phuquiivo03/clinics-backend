
// import express from 'express';
// import {appoinmentController} from '../../controllers';
// // import { authMiddleware } from '../../middleware/auth';

// const router = express.Router();

// // All routes are protected with authMiddleware in the main server file

// // User appointment routes
// router.get('/user', appoinmentController.getUserAppointments);
// router.get('/doctor', appoinmentController.getDoctorAppointments);
// router.post('/', appoinmentController.createAppointment);
// router.put('/:id/status', appoinmentController.updateAppointmentStatus);
// router.put('/:id/medical', appoinmentController.updateMedicalInfo);
// router.delete('/:id', appoinmentController.deleteAppointment);

// export const appointmentRoutes = router;
